# ПМООС-RAG v0.11.0 — зависимости.
# ВАЖНО: PyTorch ставится ОТДЕЛЬНО и ПЕРВЫМ (install.bat), из CUDA-индекса:
#   pip install torch==2.6.0 --index-url https://download.pytorch.org/whl/cu124
# Здесь torch НЕ указан намеренно, чтобы pip не подтянул CPU-сборку как зависимость.
# Нужна версия torch>=2.6 — это закрывает CVE-2025-32434 (safetensors-загрузка весов).

# --- интерфейс / запуск ---
streamlit>=1.36

# --- RAG: векторная база, эмбеддинги, реранкер, гибридный поиск ---
qdrant-client>=1.9
sentence-transformers>=3.0
transformers>=4.41
accelerate>=0.30
rank-bm25>=0.2.2

# --- разбор документов / OCR ---
pymupdf>=1.24
pdfplumber>=0.11
python-docx>=1.1
openpyxl>=3.1
pandas>=2.0
pytesseract>=0.3.10        # требует системный Tesseract OCR (для сканов; необязательно)
pillow>=10.0

# --- граф связей и визуализация ---
networkx>=3.2
pyvis>=0.3.2

# --- конфигурация / ключи ---
python-dotenv>=1.0
pyyaml>=6.0

# --- провайдеры ИИ ---
openai>=1.30               # совместим с DeepSeek (base_url) и OpenAI
anthropic>=0.30            # Claude
google-generativeai>=0.7   # Gemini
requests>=2.31             # Ollama REST + прочее

# --- утилиты ---
tqdm>=4.66
